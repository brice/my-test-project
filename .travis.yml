language: node_js
node_js:
        - "node"
script:
        - node_modules/.bin/jshint src
	- node_modules/.bin/jsdoc src
        - npm test
deploy:
        provider: pages
        skip_cleanup: true
        github_token: $GITHUB_TOKEN
        keep_history: true
        on:
            branch: master
