language: node_js
node_js: 
  - "node"
script:
  - node_modules/.bin/jshint src
  - npm test
deploy:
  provider: pages
  skip-cleanup: true
  github-token: $token
  keep-history: true
  on:
  branch: master

