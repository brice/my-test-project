language: node_js
node_js:
  - "node"

script:
  - echo “toto”
  - npm update
  - npm run test
  - node_modules/.bin/jshint src

deploy:
  provider: pages
  skip_cleanup: true
  github_token: $token  # travis ci/settings
  keep_history: true
  on:
      branch: master